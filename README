The vncsetup.sh script is designed to help users of RealVNC VNC Connect 
license and perform basic server configuration on Linux systems.

INSTRUCTIONS

1. Open a terminal window

2. Change directory to the download location of vncsetup.tar.gz

3. Uncompress and untar the tarball using:
tar xfz vncsetup.tar.gz

4. Make the script executable:
sudo chmod u+x ./vncsetup.sh

5. Execute the script :
sudo ./vncsetup.sh

 

